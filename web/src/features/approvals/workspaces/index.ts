/**
 * features/approvals/workspaces — unified approval queue.
 *
 * /approvals → priority-first queue, card-style items, batch actions.
 */

export { default as ApprovalQueueWorkspace } from "@/components/approvals-page";
