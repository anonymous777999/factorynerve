/**
 * features/billing/workspaces — billing & plans surfaces.
 *
 * /billing → subscription + invoice history + usage
 * /plans   → plan picker
 */

export { default as BillingWorkspace } from "@/components/billing-page";
export { default as PlansWorkspace } from "@/components/plans-page";
