/**
 * features/settings/workspaces — settings surfaces.
 *
 * /settings            → general settings (multi-tab)
 * /settings/attendance → attendance configuration
 */

export { default as SettingsWorkspace } from "@/components/settings-page";
export { default as SettingsAttendanceWorkspace } from "@/components/settings-attendance-page";
