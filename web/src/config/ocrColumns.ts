// IMPORTANT: These are column header indices, not field names.
// The OCR system uses string[][] where each row is an array of cell values.
// Visible columns will be determined by the headers array length.
// This constant can be used to define which column indices to show/hide in future.

// For now, we show all columns. Adjust indices here to hide specific columns.
export const VISIBLE_COLUMN_INDICES: number[] | null = null; // null = show all columns
