/**
 * shared/layouts — full-page layout primitives.
 *
 * AppShell wraps every signed-in route. AuthShell wraps unauthenticated.
 * SettingsShell wraps the tabbed settings surface.
 */

export { AppShell } from "@/components/app-shell";
export { AuthShell } from "@/components/auth-shell";
export { AuthWorkstationShell } from "@/components/auth-workstation-shell";
export { SettingsShell } from "@/components/settings-shell";
